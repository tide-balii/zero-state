# Google Gemini API key used by the Express backend for text sanitization
GEMINI_API_KEY=your_gemini_api_key_here

# Midnight Network ID requested by the frontend connector (e.g., preview)
VITE_MIDNIGHT_NETWORK=preview

# Port number for the Express backend server
PORT=3000
